# UI 设计规范 · 合同管理系统

> 版本：1.0
> 适用范围：CMC-Frontend 全部前端页面与组件
> 技术栈：Vue 3 + Element Plus + Vite

---

## 一、设计语言：青峦 (Mountain Green)

### 1.1 设计理念

以自然山峦为意象，传递沉稳、信赖、生长的品牌感受。整体风格**克制内敛**，避免过度装饰，
强调信息层级清晰、操作路径短、阅读负担低的专业级企业后台体验。

| 关键词 | 说明 |
|---|---|
| 沉稳 | 低饱和森林绿主色，传递合同管理的严肃性与可信赖感 |
| 生长 | 翡翠绿辅助色，暗示合同流程从起草到签订的"生长"过程 |
| 亲和 | 大地暖灰底色 + 大圆角，降低后台系统的冰冷感 |
| 清晰 | 克制的灰阶文字层级，明确的信息对比度 |

### 1.2 色彩系统

```
┌─────────────────────────────────────────────────────────┐
│  色阶      变量名          色值       用途               │
├─────────────────────────────────────────────────────────┤
│  主色       --c-pri        #2d6a4f   主按钮、选中态       │
│  辅色       --c-pri2       #40916c   悬停、聚焦、链接     │
│  浅色底     --c-pri-light  #e8f5e9   选中背景、标签底色   │
│  页面底色   --c-bg         #f5f7f3   全局页面背景         │
│  卡片白     --c-surface    #ffffff   卡片、面板、表格     │
│  正文色     --c-text       #1e3a2f   标题、正文           │
│  次要文字   --c-text2      #6b7c6e   辅助说明、标签       │
│  边框       --c-border     #dde5db   输入框、分割线       │
│  浅边框     --c-border-light #e8ede5 轻分割              │
│  悬停底     --c-hover      #f0faf3   行悬停、按钮悬停     │
│  成功       --c-success    #16a34a   成功状态             │
│  警告       --c-warn       #d97706   警告状态             │
│  危险       --c-danger     #dc2626   删除、拒绝           │
│  信息       --c-info       #0c4a6e   信息提示             │
└─────────────────────────────────────────────────────────┘
```

**色彩使用禁忌：**
- 正文区域禁止使用纯黑 `#000`，统一用 `#1e3a2f`
- 主色不用于大面积背景，仅用于强调元素
- 同一视图内不超过 3 种语义色彩同时出现

### 1.3 圆角规范

| 元素类型 | 圆角值 | 示例 |
|---|---|---|
| 卡片/面板 | `10px` | 统计卡片、表格容器 |
| 按钮 | `10px` | 主按钮、次按钮 |
| 输入框 | `10px` | Input、Select、DatePicker |
| 标签/Badge | `20px` (胶囊) | 状态标签 |
| 小按钮 | `6px` | 表格内操作按钮 |
| 头像 | `50%` (圆形) | 用户头像 |

### 1.4 阴影规范

| 层级 | 阴影值 | 用途 |
|---|---|---|
| 一级 (浮起) | `0 1px 3px rgba(0,0,0,.04)` | 卡片、表格 |
| 二级 (弹出) | `0 4px 12px rgba(0,0,0,.06)` | 下拉菜单、弹窗 |
| 三级 (模态) | `0 8px 24px rgba(0,0,0,.10)` | 对话框、抽屉 |

### 1.5 字体排印

| 层级 | 字号 | 字重 | 颜色 | 用途 |
|---|---|---|---|---|
| H1 | `20px` | `700` | `--c-text` | 页面标题 |
| H2 | `16px` | `600` | `--c-text` | 区块标题 |
| H3 | `14px` | `600` | `--c-text` | 卡片标题 |
| Body | `13px` | `400` | `--c-text` | 正文、表格内容 |
| Caption | `12px` | `400` | `--c-text2` | 辅助说明 |
| Small | `11px` | `400` | `--c-text2` | 标签、脚注 |
| Table Header | `11px` | `600` | `--c-text2` | 表头 (uppercase) |

字体栈：`-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif`

### 1.6 间距系统

采用 4px 基准网格：

| Token | 值 | 用途 |
|---|---|---|
| `xs` | `4px` | 图标与文字间距 |
| `sm` | `8px` | 标签间距、紧凑排列 |
| `md` | `12px` | 表单行间距 |
| `lg` | `16px` | 卡片内边距 |
| `xl` | `20px` | 内容区内边距 |
| `2xl` | `24px` | 页面外边距、区块间距 |

### 1.7 图标规范

- 图标库：Element Plus Icons (`@element-plus/icons-vue`)
- 默认尺寸：`16px` (行内)，`20px` (独立图标)
- 颜色：继承文字颜色或使用 `--c-pri` / `--c-text2`
- 菜单图标：始终与文字同行，间距 `8px`

### 1.8 Element Plus 主题定制

通过 CSS 变量覆盖 Element Plus 默认主题：

```css
:root {
  --el-color-primary: #2d6a4f;
  --el-color-primary-light-3: #4a8a6c;
  --el-color-primary-light-5: #6ba886;
  --el-color-primary-light-7: #a3d1b8;
  --el-color-primary-light-8: #c8e6d2;
  --el-color-primary-light-9: #e8f5e9;
  --el-color-primary-dark-2: #1b4332;
  --el-border-radius-base: 10px;
  --el-border-radius-small: 6px;
  --el-font-size-base: 13px;
}
```

---

## 二、排布语言：标签页工作区 (Tabbed Workspace)

### 2.1 布局结构

```
┌──────────────────────────────────────────────────────────┐
│  BRAND        [Tab1 ×] [Tab2 ×] [Tab3 ×]    👤 用户 注销 │  ← 顶部标签栏 (56px)
├────────────┬─────────────────────────────────────────────┤
│  功能导航树 │                                             │
│            │          当前标签页内容区                      │
│  · 全部合同 │                                             │
│  · 起草合同 │    页面标题 + 说明                           │
│  · 待会签 3 │                                             │
│  · 待定稿 1 │    筛选栏 / 工具栏                           │
│  · 待审批 2 │                                             │
│  · 待签订   │    数据表格 / 表单 / 详情                     │
│  · 已归档   │                                             │
│  ───────── │                                             │
│  · 客户列表 │                                             │
│  · 查询统计 │                                             │
│  · 角色管理 │                                             │
│            │                                             │
└────────────┴─────────────────────────────────────────────┘
```

### 2.2 布局参数

| 区域 | 尺寸 | 说明 |
|---|---|---|
| 顶部标签栏高度 | `56px` | 固定高度，sticky 定位 |
| Brand 区域宽度 | `200px` | 左侧品牌/Logo 区域 |
| 左侧导航宽度 | `200px` | 功能树，固定宽度 |
| 内容区最小宽度 | `auto` | 自适应剩余空间 |
| 标签页 tab 内边距 | `0 16px` | 水平方向 |
| 内容区内边距 | `24px` | 统一内边距 |

### 2.3 标签页交互规则

1. **打开标签**：点击左侧导航树中的功能项，打开对应标签页
2. **切换标签**：点击标签页切换内容区，当前标签高亮（底部 `3px` 主色边框）
3. **关闭标签**：点击标签上的 `×` 关闭，最后一个标签不可关闭
4. **标签溢出**：标签过多时，超出部分可左右滚动，不折叠
5. **状态保持**：切换标签时保持各标签页的独立状态（筛选条件、滚动位置等）

### 2.4 导航树规则

- 使用递归树结构展示功能层级
- 当前激活项高亮：背景 `--c-pri-light`，右边框 `3px solid --c-pri2`
- 数字徽标(待办数)：右对齐，颜色随紧急程度变化（红=审批、黄=会签）
- 分组标题：字体 `10px uppercase`，颜色灰色，不可点击
- 分组间用 `1px` 浅色分割线隔开

### 2.5 响应式策略

本项目为桌面端后台管理系统，**不要求移动端适配**。

| 断点 | 最小宽度 | 策略 |
|---|---|---|
| Desktop | `1280px` | 完整双栏 + 标签页 |
| Tablet (可选) | `1024px` | 图标栏替代文字导航 |

---

## 三、组件设计规范

### 3.1 按钮

| 类型 | 样式 | 用途 |
|---|---|---|
| 主按钮 | 填充 `--c-pri`，白字 | 提交、确认、保存 |
| 次按钮 | 白底 + 边框 `#c8e6c9`，绿字 | 取消、重置、次要操作 |
| 文字按钮 | 无背景，`--c-pri2` 文字 | 表格行操作（编辑、详情、删除） |
| 危险按钮 | 无背景，`--c-danger` 文字 | 删除、拒绝、注销 |

按钮高度统一 `34px`，小按钮 `28px`。同一行按钮间距 `8px`。

### 3.2 表格

```css
/* 表格设计要点 */
- 表头背景: #f9fbf7，下边框 2px solid #c8e6c9
- 行悬停背景: #fafff8
- 行高: 44px (含 padding)
- 单元格内边距: 11px 14px
- 空状态: 居中显示，灰色文字 + 插画图标
- 分页: 右对齐，紧凑模式
```

### 3.3 表单

```css
/* 表单设计要点 */
- Label 对齐: 右对齐，宽度 100px
- 输入框高度: 36px
- 必填标识: 红色星号 `*` 在 label 左侧
- 验证错误: 输入框红色边框 + 下方红色提示文字
- 表单间距: 行间距 16px
- 按钮区: 左对齐，主按钮在前
```

### 3.4 状态标签 (Badge)

| 状态 | 颜色搭配 | 示例 |
|---|---|---|
| 起草 | 浅绿底 `--c-pri-light` + 深绿字 `--c-pri` | `起草中` |
| 会签中 | 浅黄底 `#fef3c7` + 深黄字 `#92400e` | `待会签` |
| 审批中 | 浅蓝底 `#e0f2fe` + 深蓝字 `#0c4a6e` | `审批中` |
| 已完成 | 浅绿底 + 深绿字 | `已完成` |
| 已拒绝 | 浅红底 `#fee2e2` + 深红字 `#991b1b` | `已拒绝` |

### 3.5 弹窗与抽屉

- 弹窗 (Dialog)：宽度 `520px`（标准）/ `720px`（大表单），居中弹出
- 抽屉 (Drawer)：宽度 `480px`，右侧滑出
- 背景遮罩：`rgba(0,0,0,.3)`
- 关闭方式：点击遮罩、右上角 `×`、ESC 键

---

## 四、页面级设计原则

### 4.1 信息层级

每个内容页面遵循：**标题 → 说明文字 → 筛选/操作栏 → 数据展示** 的四层结构。

### 4.2 空状态

列表无数据时，显示居中的空状态提示：
- 图标/插画 (灰色调)
- 主文案："暂无数据"
- 副文案：引导用户操作的说明
- 操作按钮 (可选)：如"新建合同"

### 4.3 加载状态

- 骨架屏 (Skeleton) 用于首次加载，灰色占位块脉冲动画
- 局部 Loading 用于按钮提交后，按钮内显示旋转图标 + "提交中..."

### 4.4 反馈提示

- 操作成功：顶部绿色 Message，自动 3s 消失
- 操作失败：顶部红色 Message，含错误原因
- 危险操作：弹出确认框 (MessageBox)，标题含警告图标

### 4.5 权限可见性

- 无权限的功能入口：不显示（而非置灰）
- 无权限的按钮：不渲染
- 无权限的路由：不注册

---

## 五、代码实现规范

### 5.1 CSS 变量集中管理

所有颜色、圆角、阴影等设计 Token 统一在 `src/styles/variables.css` 中定义：

```css
:root {
  /* 详见第一章色彩系统 */
}
```

### 5.2 组件 File 结构

```
src/
├── layout/
│   ├── WorkspaceLayout.vue    # 标签页工作区主布局
│   ├── BrandHeader.vue         # 顶部品牌/Label 栏
│   ├── TabBar.vue              # 标签页栏
│   ├── NavTree.vue             # 左侧功能导航树
│   └── UserMenu.vue            # 用户信息下拉菜单
├── views/
│   ├── contract/               # 合同管理
│   ├── query/                  # 查询统计
│   ├── customer/               # 客户管理
│   └── system/                 # 系统管理
├── components/
│   ├── common/                 # 通用组件
│   └── business/               # 业务组件
└── stores/
    └── tabs.js                 # 标签页状态管理 (Pinia)
```

### 5.3 标签页状态管理

使用 Pinia Store 管理标签页状态：

```js
// stores/tabs.js
{
  openedTabs: [],      // 已打开标签 [{id, title, path, closable}]
  activeTab: null,     // 当前激活标签 ID
  // actions: openTab, closeTab, setActiveTab
}
```

---

## 六、参考示例

详细的可视化示例参见项目根目录下的 `layout-templates.html`，打开后选择标签 `6 · 标签页工作区` 查看完整布局效果。

---

## 附录 A：Element Plus 组件定制清单

| Element Plus 组件 | 定制项 |
|---|---|
| `el-menu` | 用于左侧导航树，mode="vertical"，自定义背景色 |
| `el-tabs` | 改造为顶部标签页栏，type="card" |
| `el-table` | 自定义表头样式、行悬停色、斑马纹 |
| `el-button` | 统一圆角 `10px`，调整 padding |
| `el-input` / `el-select` | 统一边框色、聚焦色、圆角 |
| `el-tag` | 自定义各状态的 type 颜色映射 |
| `el-pagination` | 紧凑模式，右对齐 |
| `el-dialog` / `el-drawer` | 自定义宽度、圆角 |
| `el-message` / `el-message-box` | 使用默认即可，颜色自动继承主题色 |

